export enum EVENT_TABS_FILTERS_BROADCAST {
  allEvents = 'all events',
  includeBroadcasted = 'broadcasted',
  includeUnbroadcasted = 'un-Broadcasted',
}

export enum EVENT_FILTERS_STATUS {
  available = 'available',
  sold = 'sold',
  expired = 'expired',
}
